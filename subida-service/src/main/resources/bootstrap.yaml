spring:
  application:
    name: subida-service
  cloud:
    config:
      enabled: true
      fail-fast: true
      retry:
        maxAttempts: 200
        maxInterval: 10000
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/mueblesstgohu1
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update